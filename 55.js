function solution(cards1, cards2, goal) {
    
    // 카드1 배열에서 골 배열의 i번째가 같은 단어이면 카드 뭉치 2로 넘어감
    // i번째가 같지 않으면 NO라고 나와야함
    // 카드2 배열도 동일함.
    // 카드1과 2를 왔다 갔다해야하는데..??????
    // 카드 1, 2배열의 i와 골 배열의 i가 같으면 반복문이 멈추고 넘아간다 브레이크?
    // 투두만든것처럼 true false로 나눌 수 있나?
    let card1 = 0;
    let card2 = 0;
    var answer = '';

    


    return answer;
}